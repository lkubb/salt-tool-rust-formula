# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  lookup:
    pkg:
      name: rustup
    paths:
      confdir: '.cargo'
      conffile: 'config.toml'
      xdg_dirname: 'cargo'
      xdg_conffile: 'config.toml'
    rootgroup: root
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
